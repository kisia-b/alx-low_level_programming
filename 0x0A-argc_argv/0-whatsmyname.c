#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program 
 * @argc: argument count
 * @argv: array of arguments
 *
 * Return: 0
 */
int main(int argc _attributr_((unused)), char **argv[])
{
      	printf("%s\n", argv);

	return (0);
}
